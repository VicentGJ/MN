from sympy import symbols, diff, solve, exp, sqrt, sin
from math import pi

# x = symbols('x')
# f = sqrt((x - 3)**2 + (sin(x) - 2)**2)
# df = diff(f, x)
# critical_points = solve(df, x)

# max_value = max([f.subs(x, cp) for cp in critical_points])
# max_point = (critical_points[0], max_value)
# min_value = min([f.subs(x, cp) for cp in critical_points])
# min_point = (critical_points[0], min_value)

# print(max_point)
# print(min_point)


print(sin(pi/2 + 1.7205828115060584/2))